    import XCTest
    @testable import whySPM

    final class whySPMTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(whySPM().text, "Hello, World!")
        }
    }
